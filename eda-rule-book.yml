---
- name: OpenShift Auto Recovery

  hosts: all

  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000

  rules:

    - name: Application Down

      condition: event.reason == "ApplicationDown"

      action:
        run_job_template:
          name: Scale-Nginx
          organization: Default
